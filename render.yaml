services:
  # 🌾 Unified Web Service (Node/Express serves React App)
  - type: web
    name: kisanmithra
    env: node
    buildCommand: npm run install-all && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
