{
  "name": "smartprocure-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --production=false --prefix backend && npm install --production=false --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  }
}
